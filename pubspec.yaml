name: wallet4d
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=2.0.0'

dependencies:
  dio: ^4.0.0

  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  hive: ^2.0.4
  hive_flutter: ^1.0.0

  intl: ^0.17.0

  qr_code_scanner:
    git:
      url: https://github.com/Pocket4D/qr_code_scanner
      ref: grab-fixes

  webview_flutter: ^2.0.4

dev_dependencies:
  build_runner: ^2.0.3
  hive_generator: ^1.1.0

flutter:
  uses-material-design: true
  generate: true

  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
